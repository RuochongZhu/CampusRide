# 🚀 前端服务启动指南

## ⚠️ 当前状态

前端服务需要手动启动。后端服务已正常运行。

---

## 📝 手动启动步骤

### 方法 1: 使用终端（推荐）

**步骤 1: 打开新的终端窗口**

在 macOS 上：
- 按 `Command + 空格`
- 输入 "Terminal" 或"终端"
- 按 Enter

**步骤 2: 进入项目目录**

```bash
cd /Users/xinyuepan/Desktop/intergration-backup_副本
```

**步骤 3: 启动前端服务**

```bash
npm run dev
```

**步骤 4: 等待启动完成**

你会看到类似这样的输出：

```
  VITE v4.5.0  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h to show help
```

**步骤 5: 访问前端**

看到上面的消息后，在浏览器打开：
```
http://localhost:5173
```

---

### 方法 2: 使用启动脚本

**在终端运行：**

```bash
cd /Users/xinyuepan/Desktop/intergration-backup_副本
bash start-frontend.sh
```

---

## ✅ 验证服务已启动

### 方法 1: 查看终端输出

如果看到这样的消息，说明成功了：
```
➜  Local:   http://localhost:5173/
```

### 方法 2: 浏览器测试

在浏览器打开 http://localhost:5173

- ✅ **成功**: 看到 CampusRide 登录页面
- ❌ **失败**: 显示"无法访问此网站"或"ERR_CONNECTION_REFUSED"

---

## 🔍 常见问题

### Q1: 端口被占用

**错误信息：**
```
Port 5173 is in use, trying another one...
```

**解决方案：**
```bash
# 杀死占用端口的进程
pkill -f vite

# 或者找到并杀死特定进程
lsof -i :5173
# 记下 PID，然后
kill -9 <PID>

# 重新启动
npm run dev
```

### Q2: npm 命令未找到

**错误信息：**
```
command not found: npm
```

**解决方案：**
检查 Node.js 是否安装：
```bash
node --version
npm --version
```

如果未安装，请安装 Node.js: https://nodejs.org/

### Q3: 依赖包缺失

**错误信息：**
```
Error: Cannot find module 'xxx'
```

**解决方案：**
```bash
cd /Users/xinyuepan/Desktop/intergration-backup_副本
npm install
npm run dev
```

### Q4: 启动后仍无法访问

**检查步骤：**

1. **确认服务确实在运行**
   ```bash
   lsof -i :5173
   ```
   应该能看到进程信息

2. **检查防火墙设置**
   确保防火墙没有阻止本地端口

3. **尝试其他端口**
   编辑 `vite.config.js`，添加：
   ```javascript
   export default defineConfig({
     server: {
       port: 3000
     }
   })
   ```

---

## 📱 启动成功后

### 1. 登录账户

```
📧 邮箱: alice@cornell.edu
🔐 密码: alice1234
```

### 2. 查看新功能

1. 点击顶部导航 **"Carpooling"**
2. 看到三个标签：
   ```
   [ Passenger ]  [ Driver ]  [ My Trips ]
   ```
3. 点击 **"My Trips"** 查看所有行程

### 3. 功能验证

确认以下内容：
- ✅ 标题显示 "My Trips"（不是 "My Bookings"）
- ✅ 每个行程显示角色徽章（Driver/Passenger）
- ✅ 状态用不同颜色显示
- ✅ 行程按时间排序

---

## 🛠️ 后端服务状态

后端服务已正常运行：
- ✅ 地址: http://localhost:3001
- ✅ 状态: 正常
- ✅ My Trips API: 已启用

---

## 📞 需要帮助？

如果仍然无法启动，请尝试：

1. **完全重启**
   ```bash
   # 杀死所有相关进程
   pkill -f vite
   pkill -f node
   
   # 重新启动后端
   cd /Users/xinyuepan/Desktop/intergration-backup_副本/campusride-backend
   npm run dev
   
   # 在另一个终端启动前端
   cd /Users/xinyuepan/Desktop/intergration-backup_副本
   npm run dev
   ```

2. **重新安装依赖**
   ```bash
   cd /Users/xinyuepan/Desktop/intergration-backup_副本
   rm -rf node_modules
   npm install
   npm run dev
   ```

3. **检查 Node.js 版本**
   ```bash
   node --version
   ```
   建议使用 Node.js 16+ 版本

---

## 🎯 快速命令参考

```bash
# 进入项目目录
cd /Users/xinyuepan/Desktop/intergration-backup_副本

# 启动前端
npm run dev

# 检查端口
lsof -i :5173

# 停止服务
# 按 Ctrl + C（在运行 npm run dev 的终端中）

# 或杀死进程
pkill -f vite
```

---

**最后更新**: 2025-11-04
**项目**: CampusRide
**功能**: My Trips 系统







