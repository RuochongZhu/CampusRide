# Test Environment Variables for Local Testing
NODE_ENV=test
PORT=3001

# Database (will create if not exists)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/campusride_test

# JWT Secret for testing
JWT_SECRET=test_jwt_secret_key_for_local_testing

# Mock API keys for testing (not real keys)
RESEND_API_KEY=test_resend_api_key
SUPABASE_URL=https://test.supabase.co
SUPABASE_ANON_KEY=test_supabase_anon_key

# CORS
FRONTEND_URL=http://localhost:5173